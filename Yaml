services:
  - type: web
    name: sosoagent-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python sosoagent.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SOSO_API_KEY
        sync: false
